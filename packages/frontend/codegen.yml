

overwrite: true
schema: "http://localhost:4000/api"
documents: 'src/api/operations/**/*.{gql,graphql}'
generates:
  src/api/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
